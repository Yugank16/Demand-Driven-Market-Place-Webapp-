import React from 'react';

const RequestItem = (props) => (
    <div>
        <div className="request-list">
            <div>
                {props.data}
            </div>
        </div>
    </div>

);


export default RequestItem;
